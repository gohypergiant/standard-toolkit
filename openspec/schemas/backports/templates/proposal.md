## Why

<!-- Explain the motivation for this change. 
     What problem does this solve? 
     What value does the backport provide over building from scratch? -->

## What Changes

<!-- Describe what will change. Be specific about new capabilities and modifications. -->

## Capabilities

### New Capabilities

<!-- Capabilities being introduced. Replace <name> with kebab-case identifier (e.g., user-auth, data-export, api-rate-limiting). Each creates specs/<name>/spec.md -->
- `<name>`: <brief description of what this capability covers>

### Modified Capabilities
<!-- Existing capabilities whose REQUIREMENTS are changing (not just implementation).
     Only list here if spec-level behavior changes. Each needs a delta spec file.
     Use existing spec names from openspec/specs/. Leave empty if no requirement changes. -->
- `<existing-name>`: <what requirement is changing>

## Impact

### Code Changes

<!-- Which packages change, and what kind of change (new files, new exports, etc.). -->

### Dependencies

<!-- New dependencies added and why (peer, optional, or bundled). -->

### APIs

<!-- New public API surface. Note whether changes are additive (non-breaking)
     or modify existing APIs (breaking). -->

### Other Systems

<!-- Anything else affected: documentation, tooling, dependent packages, etc.
     Remove this section if none. -->
