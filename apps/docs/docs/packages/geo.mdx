## Coordinate Parsing

import Playground from '../../components/playground';
import { coordinateSystems, createCoordinate } from '../../../../packages/geo/src';

<Playground files={{
  '@accelint/geo': { coordinateSystems, createCoordinate },
}}>
```ts
import { coordinateSystems, createCoordinate } from '@accelint/geo';

const coordinate = createCoordinate(coordinateSystems.dd, 'LATLON');

coordinate('32.1 54.321').dms();
```
</Playground>
