{
  "name": "@accelint/docs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm run gen && pnpm run build:ladle",
    "build:ladle": "pnpm --filter=@accelint/design-system exec ladle build -o ../../apps/docs/dist/ladle --base ladle",
    "gen": "typedoc",
    "serve": "http-server ./dist -p 3000"
  },
  "dependencies": {
    "@accelint/design-system": "workspace:*",
    "http-server": "14.1.1",
    "typedoc": "0.26.11"
  }
}
