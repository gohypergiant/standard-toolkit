# Curated gold set for __TARGET_NAME__. HUMAN-VERIFIED entries only.
# corpus_hash detects drift: if the corpus changes, supporting_passages may rot.
# It is content-based (see corpus_hash.py — mirrors bootstrap_goldset.py).
# PLACEHOLDER — not a real hash. When arming the drift test, compute the real
# 16-hex value with:  python corpus_hash.py <corpus_dir>
corpus_hash: "PLACEHOLDER-RUN-corpus_hash.py"
# Set corpus_path (absolute, or relative to this evals/ dir) to arm the
# corpus-drift test (tests/test_corpus_drift.py). The demo leaves it unset,
# so the drift test skips.
# corpus_path: "../docs"
entries:
  - id: q1
    question: "What is the maximum payload mass for the X-9 platform?"
    answer: "1,200 kg."
    supporting_passages:
      - {chunk_id: "x9-spec#c318", doc: "x9-spec.pdf", page: 42}
    answerable: true
    verified: true
  - id: q2
    question: "What is the X-9 cruise altitude?"
    answer: "12,000 m."
    supporting_passages:
      - {chunk_id: "x9-spec#c204", doc: "x9-spec.pdf", page: 27}
    answerable: true
    verified: true
  # Adversarial: NOT in the corpus → the bot must refuse, not invent.
  - id: q_adv1
    question: "What is the warranty period for the X-9 platform?"
    answer: null
    supporting_passages: []
    answerable: false
    verified: true
