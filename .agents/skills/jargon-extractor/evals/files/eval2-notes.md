# Release Notes - v1.4

This release introduces canary deploys: a new build first receives a
small percentage of production traffic before being rolled out to
everyone, so a bad release only affects a few users instead of all of
them.

As a reminder, DLQ (dead-letter queue) behavior is unchanged in this
release.
