# Epistemic Map

_Last updated: {{DATE}} · {{FACT_COUNT}} facts · {{QUESTION_COUNT}} open questions · {{ASSUMPTION_COUNT}} assumptions · {{RISK_COUNT}} risks · {{RESOLVED_COUNT}} resolved since last run_

This file is entirely generated by `scripts/merge_epistemic_map.py`, which
also parses it back in as state on the next run — there is no frontmatter,
no companion file, and nothing stored twice. Never hand-edit this file —
see SKILL.md's "script-only" note.

## Facts (Known Known)

### EM-001 · {{truncated statement, ~70 chars}}

Confidence: {{CONFIRMED|INFERRED|CONFLICTING}}
Category: {{category slug}}
Severity: {{high|medium|low}}
First seen: {{DATE}}
Evidence: `{{source}}:{{location}}` ({{N}} ref{{s}})

{{full statement text}}

**Why it matters:** {{impact}}

**Evidence notes:**
- `{{source}}:{{location}}` -- {{detail}}

<!-- Risk entries (Unknown Unknown) replace "Evidence: ..." with
     "Sources considered: path1, path2, ..." in the metadata block, and
     replace the closing "**Evidence notes:**" bullets with a
     "**Reasoning:**" paragraph instead. A promoted entry gains a
     "History (DATE): was -> became — reason" metadata line; a resolved
     or dismissed entry gains a "Status: resolved — note" line. Every
     entry across all four quadrants uses this same four-paragraph shape:
     metadata block, full statement, "Why it matters", and a closing
     Evidence notes / Reasoning section. -->

{{EXECUTIVE_SUMMARY}}

## Knowledge Matrix

```
                    HIGH CERTAINTY (validated)
                              |
          [ ASSUMPTIONS ]    |    [ FACTS ]
          {{ASSUMPTION_COUNT}} open       |    {{FACT_COUNT}} confirmed
                              |
    ------------------------ + ------------------------
                              |
          [ RISKS ]           |    [ QUESTIONS ]
          {{RISK_COUNT}} open            |    {{QUESTION_COUNT}} open
                              |
                    LOW CERTAINTY (unvalidated)
```

## Facts (Known Known)

{{#each facts}}
### [{{id}}] {{statement}} ({{category}} · {{confidence}})
Evidence: {{evidence}}
Impact: {{impact}}
{{#if promoted_from}}History: was {{promoted_from}}, promoted {{promoted_date}} — {{promotion_reason}}{{/if}}

{{/each}}

## Questions (Known Unknown)

{{#each questions}}
### [{{id}}] {{statement}} ({{category}} · {{confidence}})
Evidence: {{evidence}}
Impact: {{impact}}
Status: {{status}}{{#if resolution_note}} — {{resolution_note}}{{/if}}

{{/each}}

## Assumptions (Unknown Known)

{{#each assumptions}}
### [{{id}}] {{statement}} ({{category}} · {{confidence}})
Evidence: {{evidence}}
Impact: {{impact}}
Status: {{status}}{{#if resolution_note}} — {{resolution_note}}{{/if}}

{{/each}}

## Risks (Unknown Unknown)

{{#each risks}}
### [{{id}}] {{statement}} ({{category}} · {{confidence}})
Reasoning: {{reasoning}}
Impact: {{impact}}
Status: {{status}}{{#if resolution_note}} — {{resolution_note}}{{/if}}

{{/each}}

## Epistemic Backlog

{{#each backlog_items}}
- [ ] {{action}} — resolves [{{source_id}}] ({{severity}})
{{/each}}
