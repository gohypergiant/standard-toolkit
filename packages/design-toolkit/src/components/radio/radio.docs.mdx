import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';

import * as RadioStories from './radio.stories';

<Meta of={RadioStories} />

# Radio

Radio buttons allow users to select one option from a group of mutually exclusive choices.

```tsx
<RadioGroup label="Select an option">
  <Radio value="1">Option 1</Radio>
  <Radio value="2">Option 2</Radio>
  <Radio value="3">Option 3</Radio>
</RadioGroup>
```

<Canvas of={RadioStories.Default} />
<Controls of={RadioStories.Default} />

## Component Structure

- **RadioGroup**: Container that manages selection state and layout
- **Radio**: Individual radio button option

## RadioGroup Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `label` | `ReactNode` | — | Label displayed above the radio group |
| `orientation` | `'vertical' \| 'horizontal'` | `'vertical'` | Layout direction of radio buttons |
| `value` | `string` | — | Controlled selected value |
| `defaultValue` | `string` | — | Default selected value (uncontrolled) |
| `onChange` | `(value: string) => void` | — | Called when selection changes |
| `isDisabled` | `boolean` | `false` | Disables all radio options |
| `isRequired` | `boolean` | `false` | Marks selection as required |
| `classNames` | `object` | — | Custom classes: `group`, `label` |

## Radio Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `string` | — | Value associated with this option |
| `isDisabled` | `boolean` | `false` | Disables this specific option |
| `classNames` | `object` | — | Custom classes: `radio`, `control`, `label` |

## Accessibility

- Arrow keys navigate between options
- Space/Enter selects the focused option
- Automatic ARIA attributes for screen readers
- Focus management and form integration

## Related

- [React Aria RadioGroup](https://react-spectrum.adobe.com/react-aria/RadioGroup.html)
- [GitHub Source](https://github.com/gohypergiant/standard-toolkit/tree/main/packages/design-toolkit/src/components/radio)
