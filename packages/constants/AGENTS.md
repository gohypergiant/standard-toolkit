<!-- Inherits from: ../../AGENTS.md -->
<!-- Only package-specific overrides and additions are defined here. -->

# Agent Behavior for @accelint/constants

Follow the root [AGENTS.md](../../AGENTS.md). This file adds package-specific rules only.

- Read `./ARCHITECTURE.md` and `./README.md` before changing this package.
- Keep this package limited to shared static data; do not add runtime helpers, state, or behaviorful utilities here.
- Treat exported constants, regexes, and documented defaults as compatibility surface. Ask before semantic changes.
- If constant semantics change, update the README examples in the same change.
