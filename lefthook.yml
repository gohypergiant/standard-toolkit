pre-commit:
  commands:
    format:
      # NOTE: this also will license and index
      run: pnpm run format && pnpm run format:deps
      stage_fixed: true

pre-push:
  commands:
    validate-docblocks:
      run: pnpm run audit:docblocks || true
