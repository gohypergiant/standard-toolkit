pre-commit:
  commands:
    format:
      run: pnpm run format && pnpm run format:deps
      stage_fixed: true
      priority: 1
    validate-docblocks:
      run: pnpm run audit:docblocks || true
      priority: 2

