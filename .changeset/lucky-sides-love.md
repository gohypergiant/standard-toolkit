---
"@accelint/design-toolkit": minor
---

Tabs (& Tab) components now have two new props:
- `align` which changes the alignment of content within a tab
- `flex` which causes the tab to grow up to a maximum dimension of 200px
