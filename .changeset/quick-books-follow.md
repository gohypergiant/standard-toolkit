---
"@accelint/map-toolkit": minor
---

Add `minElevation` support for 3D shapes. Polygon-type shapes render as floating slabs between `minElevation` and `maxElevation`. LineString curtains and Point indicator lines drop to `minElevation` instead of ground level.
