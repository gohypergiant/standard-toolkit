---
"@accelint/design-foundation": minor
"@accelint/design-toolkit": minor
---

Adds `elevation-overlay` and `elevation-raised` variants to design-foundation for use with `data-elevation` attribute

Adds `elevation` prop to the ActionBar component with options `flat`, `overlay`, and `raised` to allow configurable shadow styling
