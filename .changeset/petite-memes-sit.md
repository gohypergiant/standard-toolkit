---
"@accelint/design-toolkit": minor
---

Fix combobox select and clear issues. Add matched text highlighting to OptionsItemLabel when used inside a ComboBox, controllable via `highlight` prop (defaults to `true`).
