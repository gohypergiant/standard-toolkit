---
"@accelint/design-toolkit": patch
---

Widen react-aria ecosystem peer dependency ranges from tilde (~) to caret (^) to prevent version conflicts when react-aria-components resolves newer compatible versions of `@react-aria/*`, `@react-stately/*`, `@react-types/*`, and `@internationalized/date`.
